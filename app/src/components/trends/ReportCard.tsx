import './ReportCard.css'

const ReportCard = ({} : {}) => {

    const id = 291; 

  return (
    <div className = "report-card">
        <p className = "title">Report: {id.toString()}</p>
    </div>
  )
}

export default ReportCard